#spring:
  #datasource:
    #url: jdbc:mysql://localhost:3306/gestion_pharmacie?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    #username: root
    #password:

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmacie
    username: postgres
    password: root
